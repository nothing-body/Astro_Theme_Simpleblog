---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PostsListSection from '../../../components/PostsListSection.astro';
import { useTranslations } from '../../../i18n/utils';
import { getPostsPageStaticPaths } from '../../../lib/posts';

export async function getStaticPaths() {
  return getPostsPageStaticPaths('zh-tw');
}

const { page } = Astro.params;
const { totalPages, allPosts } = Astro.props;
const t = useTranslations('zh-tw');
const currentPage = Number(page);
---

<BaseLayout
  title={`${t('posts.title')} - 第 ${currentPage} 頁`}
  description={`所有文章列表第 ${currentPage} 頁。`}
>
  <PostsListSection
    lang="zh-tw"
    allPosts={allPosts}
    currentPage={currentPage}
    totalPages={totalPages}
  />
</BaseLayout>
