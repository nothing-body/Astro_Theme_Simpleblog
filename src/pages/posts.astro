---
import PostsPage from '../components/PostsPage.astro';
import { getPublishedPostsForLang, getTotalPages } from '../lib/posts';

const allPosts = await getPublishedPostsForLang('en');
---

<PostsPage lang="en" allPosts={allPosts} currentPage={1} totalPages={getTotalPages(allPosts.length)} />
