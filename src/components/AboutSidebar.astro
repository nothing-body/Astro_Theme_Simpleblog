---

export interface Props {
  lang?: 'zh-tw' | 'en' | 'zh-cn';
  currentPath: string;
}

const { lang = 'zh-tw', currentPath } = Astro.props;

const menuZh = [
  { label: '關於本站', path: '/zh-tw/about' },
  { label: '聯絡我們', path: '/zh-tw/contact' },
  { label: '隱私權政策', path: '/zh-tw/privacy' },
  { label: '免責聲明', path: '/zh-tw/disclaimer' },
];

const menuEn = [
  { label: 'About This Site', path: '/about' },
  { label: 'Contact Us', path: '/contact' },
  { label: 'Privacy Policy', path: '/privacy' },
  { label: 'Disclaimer', path: '/disclaimer' },
];

const menuZhCn = [
  { label: '关于本站', path: '/zh-cn/about' },
  { label: '联系我们', path: '/zh-cn/contact' },
  { label: '隐私政策', path: '/zh-cn/privacy' },
  { label: '免责声明', path: '/zh-cn/disclaimer' },
];

const menu = lang === 'en' ? menuEn : lang === 'zh-cn' ? menuZhCn : menuZh;
const sidebarLabel =
  lang === 'en' ? 'About sidebar' : lang === 'zh-cn' ? '关于侧边栏' : '關於側邊欄';
const sidebarTitle = lang === 'en' ? 'About' : lang === 'zh-cn' ? '关于本站' : '關於本站';

const isActive = (menuPath: string) => currentPath === menuPath || currentPath === menuPath + '/';
---

<aside class="sidebar" aria-label={sidebarLabel}>
  <div class="glass-card sidebar-widget">
    <h3 class="widget-title">{sidebarTitle}</h3>
    <ul class="category-list" role="list">
      {
        menu.map(item => (
          <li role="listitem">
            <a
              href={item.path}
              class:list={['cat-link', { 'cat-link--active': isActive(item.path) }]}
              aria-current={isActive(item.path) ? 'page' : undefined}
            >
              <span>{item.label}</span>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</aside>
